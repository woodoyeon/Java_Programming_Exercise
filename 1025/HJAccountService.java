package Bank_1025;

public class HJAccountService extends AccountService {
    public HJAccountService(AccountDAO accountDAO) {
        super(accountDAO);
    }

    // 추가 확장 기능 구현 (필요에 따라 추가)
}
